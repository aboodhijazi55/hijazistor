/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "images.pexels.com",
            "storage.googleapis.com",
            "cdn.dummyjson.com"
        ]
    }

};

export default nextConfig;
